<!DOCTYPE html>
<html>
  <head>
    <title>MyRorWebsite</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="page-container">
      <div class="navbar-container"><%= render "shared/navbar" %></div>
      <div class="content-area"> <!-- A new class to wrap both the side panel and the main content -->
        <div class="side-panel-container">
          <%= render 'shared/side_panel' %>
        </div>
        <div class="content-container">
          <%= render "shared/flashes" %>
          <%= yield %>
        </div>
      </div>
      <div class="footer-container"><%= render "shared/footer" %></div>
    </div>
  </body>
</html>
